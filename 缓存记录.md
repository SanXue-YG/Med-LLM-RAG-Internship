# 缓存 / 模型 / 数据集 记录（清理与整合参考）

> 用途：实习项目结束后释放磁盘；对照统一 `Dataset/` 布局。  
> 测算日期：**2026-07-16**（本机实测；体积会随后续运行变化）  
> 仓库根目录：`D:\谷歌`  
> **统一数据集目录（现行）**：`D:\谷歌\Dataset\`（见 [`Dataset/README.md`](Dataset/README.md)、[`dataset_paths.py`](dataset_paths.py)）

---

## 0. Dataset 迁移状态（2026-07-16）

| 资产 | Dataset 路径 | 迁移结果 | 旧路径兼容 |
|------|--------------|----------|------------|
| 样本 Chroma | `Dataset/chroma/chroma_db/` | ✅ 实体已迁入 | `04/.../data/chroma_db` → **目录联接** |
| 全量 Chroma | `Dataset/chroma/chroma_db_full/` | ✅ **实体已迁入**（2026-07-16 关内核后 materialize） | `04/.../data/chroma_db_full` → **目录联接** → Dataset |
| BM25 | `Dataset/bm25/bm25_full/` | ✅ 实体已迁入 | `09/.../data/bm25_full` → **目录联接** |
| slim | `Dataset/processed/oa_comm_slim.jsonl` | ✅ 实体在 Dataset；**已删** 06 硬链接名（原非双倍占盘） | `resolve_slim_path()` → Dataset；E: 可回退 |
| chunks | `Dataset/processed/oa_comm_chunks.jsonl` | ✅ 实体在 Dataset；**已删** 09 硬链接名 | `resolve_chunks_path("full")` → Dataset |

代码：`06/src/config.py` 的 `resolve_*` 与 `09/src/bm25_store.py` **优先 Dataset**。新代码请 `import dataset_paths`。

---

## 1. 一句话对照：下载了什么

| 类别 | 名称 | 性质 | 约大小 | 默认位置 |
|------|------|------|--------|----------|
| HF 嵌入模型 | `BAAI/bge-small-en-v1.5` | 权重缓存（建库/向量检索） | ~0.13 GB | `%USERPROFILE%\.cache\huggingface\hub\models--BAAI--bge-small-en-v1.5` |
| HF 重排模型 | `BAAI/bge-reranker-base` | 权重缓存（06 Cross-Encoder） | ~1.06 GB | 同上 `...\models--BAAI--bge-reranker-base` |
| HF 其它 | `sentence-transformers/all-MiniLM-L6-v2` | 早期/对比用 | ~0.09 GB | 同上 hub |
| HF 合计 | — | transformers 默认缓存 | **~1.27 GB** | `C:\Users\10138\.cache\huggingface` |
| Ollama LLM | `deepseek-r1:7b` | 本地生成模型 | **~4.36 GB** | `D:\谷歌\01 验证模型\ollama_models\` |
| 全量向量库 | Chroma `pmc_oa_comm_full` | 检索索引 | **~66–71 GB** | **`Dataset/chroma/chroma_db_full/`**（`chroma.sqlite3` ≈ **65.7 GB**） |
| 样本向量库 | Chroma `pmc_oa_comm_sample` | 开发小库 | ~0.02 GB | **`Dataset/chroma/chroma_db/`** |
| 全量 chunks | `oa_comm_chunks.jsonl` | BM25 / 语料 | **~9.12 GB** | **`Dataset/processed/oa_comm_chunks.jsonl`** |
| slim 元数据 | `oa_comm_slim.jsonl` | 重排年份/期刊回查 | **~8.29 GB** | **`Dataset/processed/oa_comm_slim.jsonl`** |
| BM25 索引 | 分片 62 片 | 关键词路离线索引 | **~15.46 GB** | **`Dataset/bm25/bm25_full/`** |
| E: 备份总盘 | `med-llm-rag-datasets` | 外盘权威备份 | **~525 GB**（整目录） | `E:\med-llm-rag-datasets\` |
| E: chroma 备份 | `chroma_db_full` | 与 D: 全量库对应 | **~65.95 GB** | `E:\med-llm-rag-datasets\chroma_db_full\` |

说明：

- Notebook 提示「加载 `BAAI/bge-reranker-base`（首次 ~1.1GB）」= HuggingFace 拉重排权重；已缓存后一般不再下载。
- 全量 live 检索读取 Chroma / BM25 / slim / chunks（本地数据，非 HF 模型）。

---

## 2. 模型类：清理方式

### 2.1 HuggingFace（约 1.3 GB）

```
C:\Users\10138\.cache\huggingface\
└── hub\
    ├── models--BAAI--bge-reranker-base      ≈ 1.06 GB   ← 06 重排
    ├── models--BAAI--bge-small-en-v1.5     ≈ 0.13 GB   ← 04/05/06 向量
    └── models--sentence-transformers--all-MiniLM-L6-v2  ≈ 0.09 GB
```

- 可整目录删除：`C:\Users\10138\.cache\huggingface`
- 删后下次跑 04/06 会重新从网络下载

### 2.2 Ollama（约 4.4 GB）

```
D:\谷歌\01 验证模型\ollama_models\     ← 本项目实测存放点（~4.36 GB）
C:\Users\10138\.ollama\                ← 默认全局目录（本机几乎为空）
```

- 删除项目内 `ollama_models`，或：`ollama rm deepseek-r1:7b`

### 2.3 Conda 环境（可选）

- `conda env remove -n med-rag-verify`

---

## 3. Dataset 布局与代码入口

```
D:\谷歌\Dataset\
├── README.md
├── chroma\
│   ├── chroma_db_full\
│   └── chroma_db\
├── bm25\
│   └── bm25_full\
└── processed\
    ├── oa_comm_chunks.jsonl
    └── oa_comm_slim.jsonl
```

```python
# 新代码（推荐）
from dataset_paths import CHROMA_FULL_DIR, CHUNKS_FULL_JSONL, SLIM_JSONL, BM25_FULL_DIR

# 既有流水线
from config import resolve_chroma, resolve_chunks_path, resolve_slim_path, resolve_bm25_cache_dir
# （06 config：Dataset → legacy → E:）
```

| 变量 | 作用 |
|------|------|
| `MED_RAG_DATASET_ROOT` | 覆盖 Dataset 根 |
| `STAGE09_BM25_FULL_DIR` | 仅覆盖 BM25 目录 |

全量 Chroma 实体化（关闭占用后）：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\materialize_chroma_to_dataset.ps1
```

---

## 4. 结束项目时：清理优先级（省空间）

| 优先级 | 删什么 | 约回收 | 后果 |
|--------|--------|--------|------|
| P0 | `E:\med-llm-rag-datasets\`（确认无备份需求） | 最高 ~**525 GB** | 无法从 E: 恢复 |
| P0 | `Dataset/chroma/chroma_db_full`（及联接目标） | **~66–71 GB** | 全量向量检索不可用 |
| P1 | `Dataset/processed/*.jsonl` + `Dataset/bm25/bm25_full` | **~33 GB** | BM25/元数据不可用 |
| P2 | `01 .../ollama_models` 或 `ollama rm` | **~4.4 GB** | 需重新 pull |
| P3 | `C:\Users\10138\.cache\huggingface` | **~1.3 GB** | 下次重新下模型 |
| P4 | conda `med-rag-verify` | 数 GB+ | 需重建环境 |

**注意**：硬链接的 JSONL 删「一处」文件名不会立刻释放空间（须删掉所有硬链接名）；目录联接删联接本身不删目标数据。

---

## 5. 与阶段代码的对应关系

| 阶段 | 主要落地物 |
|------|------------|
| 01 | Ollama `deepseek-r1:7b` |
| 02–03 | slim / chunks（现归 Dataset/processed） |
| 04 | `bge-small` 缓存 + Chroma（现归 Dataset/chroma） |
| 05–06 | 读 Chroma；06 另缓存 `bge-reranker-base`；读 slim / BM25 |
| 07–08 | 调 Ollama |
| 09 | BM25 分片（现归 Dataset/bm25） |
| 10 | 复用以上 |

---

## 6. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-16 | 初稿：HF/Ollama/体积实测 |
| 2026-07-16 | 迁入 `Dataset/`；更新 `06/config` / `09/bm25_store` / README；chroma 占用时联接回退 + 实体化脚本 |
