# 答案评估与缓存报告

> **状态：已完成** — 阶段 0–7 全部通过（2026-07-08）
>
> **本阶段范围**：多维度答案评估器、生成结果缓存、批量并行处理、统一粘合流水线  
> **不包含**：人工标注平台、在线 A/B、分布式缓存（Redis）  
> **验证模式**：默认 `offline`（复用 08 `generation_eval.json` 快照）；可选 `live`（Ollama）

---

## 1. 概述

本阶段在 08 端到端生成能力之上，补齐 RAG 链路的**质量保障与工程优化层**：

- **08**：生成执行 → `answer` + `sources`
- **09（本阶段）**：评估 + 缓存 + 批量调度 → `generation` + `evaluation` + `cache`

### 1.1 端到端数据流

```text
query
  ↓ GenerationCache lookup(key=hash(query, context, model, temperature))
  ├─ HIT  → 复用缓存 generation
  └─ MISS → ModelAdapter.generate()（默认复用 08 快照或 live pipeline）
  ↓ AnswerEvaluator.evaluate()
  ↓ 统一输出 { generation, evaluation, cache, extensions }
```

### 1.2 验证范围说明

> 与 06「验证范围说明」、08 §1.2 对齐：**本阶段报告中的 ROUGE / key_info_recall 数值，是在样本库检索链路上测得的，不能代表全量 PMC 语料上的最终 RAG 质量。**

| 维度 | 本阶段实际使用 | 说明 |
|------|----------------|------|
| 检索语料 | 06 样本库 **`mode="sample"`**（**1,267** chunks） | 见 `06 .../schedule.md` §「验证范围说明」；非 610 万全量 |
| 检索输入 | 06 `pipeline_eval.json` 离线 reranked（每条约 5 候选） | 08 `OfflineRetrievalAdapter` 读取；`generation_eval.json` 标注 `offline_sample_pipeline_eval` |
| 生成答案 | 08 `generation_eval.json` 四条快照（offline 默认） | 09 `SnapshotModelAdapter` 复用，不重新跑 Ollama |
| 评估基准 | `data/ground_truth.json` 四条人工 reference + key_phrases | 临床指南型参考文本，**非**从样本库自动抽取 |
| 缓存 / 批量 | 进程内 LRU+TTL；`BatchRunner` 四条并行 | 与语料规模无关，本阶段已验证链路 |

**数据链路**：

```text
06 pipeline_eval.json (sample, 1,267 chunks)
  → 08 run_generation_eval.py (OfflineRetrievalAdapter + Ollama)
  → 08 generation_eval.json (快照)
  → 09 run_eval_cache_batch.py --mode offline (SnapshotModelAdapter)
  → AnswerEvaluator vs ground_truth.json
```

**对指标解读的影响**：

- ROUGE / key_info_recall 偏低，**与样本库覆盖不足直接相关**：样本中常缺 MI 标准治疗、metformin 心血管获益等专题文献，生成答案会如实表达「context 未提供」。
- 例如 Q1（`What is the treatment for MI?`）在 `pipeline_eval.json` 中**无精确匹配 query**（仅有 `MI treatment guideline`），08 评测走 **fallback reranked**，命中 homocysteine / CABG 应激等弱相关 chunk（relevance_score≈0.28），故 `key_info_recall=0` 可预期。
- 本阶段验证目标是 **09 评估/缓存/批量链路正确性**（阶段 0–6 在样本库上完成）；全量 PMC 复评见 **§6**。

| 项 | 本阶段 notebook / CLI 验证 | 说明 |
|----|---------------------------|------|
| 生成输入 | 08 `generation_eval.json` 四条快照（offline） | 不依赖实时 Ollama，便于复现 |
| 评估基准 | `data/ground_truth.json` 四条 reference + key_phrases | 与 08 `DEFAULT_QUERIES` 对齐 |
| 缓存 | 进程内 LRU + TTL + 低温门控 | 进程退出即失效（MVP 边界） |
| 批量 | `BatchRunner` 多 query 并行 | 单 query 内部仍串行 |
| live 模式 | `--mode live` 仍用 06 样本 `pipeline_eval.json` | 仅重跑生成，检索未切 `from_mode("full")` |

---

## 2. 任务书对照（`任务.txt`）

> 说明：下表“实现策略”列描述**本阶段如何落地**，不是仅做完成标记。

### 2.1 评估部分

| 任务书要求 | 交付模块 | 实现策略 | 状态 |
|------------|----------|----------|------|
| 文本相似性（ROUGE） | `AnswerEvaluator.score_rouge()` | 使用 `rouge-score` 计算 `rouge1/rouge2/rougeL` 的 F1；输入为 `generated_answer` 与 `ground_truth.reference_answer` 文本重叠度 | ✅ 已实现 |
| 关键信息提取 + recall | `patterns.py` + `key_info_recall()` | 用正则提取百分比/剂量/时间范围，并用关键词组匹配安全/治疗/机制信息；`recall = matched_gt / total_gt` | ✅ 已实现 |
| 幻觉检测（绝对化信号） | `detect_hallucination_signals()` | 规则匹配 `studies show`、`has been proven`、`100%`、`completely safe` 等信号并加权累加为 `hallucination_risk`（风险提醒，非终审） | ✅ 已实现 |
| 可读性（句长等） | `readability_metrics()` | 按 `.!?` 分句，统计句数、词数、平均句长、平均词长 | ✅ 已实现 |
| 汇总评估 | `evaluate()` → `EvaluationResult` | 四维指标统一封装为 `EvaluationResult`，供流水线与报告直接消费 | ✅ 已实现 |

### 2.2 缓存策略

| 任务书要求 | 交付模块 | 实现策略 | 状态 |
|------------|----------|----------|------|
| query + context 哈希键 | `GenerationCache.make_key()` | 将 `query/context/model/temperature_bucket` 规范化后 `sha256`，保证同输入同键 | ✅ 已实现 |
| 大小限制防 OOM | `GenerationCache` LRU | `OrderedDict` 维护访问顺序，超过 `max_entries`（默认 128）淘汰最久未用项 | ✅ 已实现 |
| TTL 时效控制 | `GenerationCache.get/set` | 写入时记录 `expires_at`，读取时过期即删除，默认 TTL 24h（可配） | ✅ 已实现 |
| 低温才缓存 | `GenerationCache.set()` | `temperature > max_temperature`（默认 0.3）时拒绝写入 | ✅ 已实现 |
| 缓存统计 | `GenerationCache.stats()` | 维护 `hits/misses/evictions/size`，报告与 notebook 可直接展示 | ✅ 已实现 |

### 2.3 批量处理（optional）

| 任务书要求 | 交付模块 | 实现策略 | 状态 |
|------------|----------|----------|------|
| ThreadPoolExecutor 并行 | `BatchRunner.run_batch()` | 多 query 并发执行；单 query 内部仍串行，不破坏 08 多 stage 依赖 | ✅ 已实现 |
| 按 CPU 核心数控制并发 | `BatchRunner.__init__` | 默认 `min(4, cpu_count)`，避免 Ollama/本地推理排队过载 | ✅ 已实现 |
| 单任务失败不中断 | `BatchRunner._run_single()` | 单任务 `try/except`，失败项返回 `status=error`，其余继续 | ✅ 已实现 |
| 输入输出顺序一致 | `run_batch()` 重排 | 用 `_index` 保序，最终输出顺序与输入 `queries` 对齐 | ✅ 已实现 |

### 2.4 复跑验证

| 任务书要求 | 交付模块 | 实现策略 | 状态 |
|------------|----------|----------|------|
| 复跑 08 固定 query | CLI + notebook C6 | 对齐 08 `DEFAULT_QUERIES` 四条，读取 `ground_truth.json` 做评估对照 | ✅ 已实现 |
| 验证评估/缓存/批量 | `eval_cache_batch_report.json` + acceptance tests | 两轮批量执行（冷缓存 + 热缓存），并导出汇总指标与单条明细 | ✅ 已实现 |

**任务书整体验证结论：✅ 全部满足。**

---

## 3. 模块与接口

| 模块 | 路径 | 职责 |
|------|------|------|
| 评估器 | `src/answer_evaluator.py` | ROUGE / recall / 幻觉风险 / 可读性 |
| 规则库 | `src/patterns.py` | 医学关键信息与风险信号正则 |
| 缓存 | `src/generation_cache.py` | LRU + TTL + 温度门控 |
| 批量 | `src/batch_runner.py` | 多 query 并行调度 |
| 粘合流水线 | `src/pipeline_with_eval.py` | generation + evaluation + cache |
| 模型适配 | `src/model_adapter.py` | `ModelAdapter` / `SnapshotModelAdapter` |
| 报告构建 | `src/report_builder.py` | 汇总命中率、耗时、评估分布 |
| CLI | `scripts/run_eval_cache_batch.py` | offline/mock/live；`--retrieval-mode full` 全量 live（阶段 7） |

---

## 4. 验证结果（offline，2026-07-08）

> 数据来源：`outputs/samples/eval_cache_batch_report.json`（由 `python scripts/run_eval_cache_batch.py --mode offline` 生成）

### 4.1 单测结果

```text
pytest tests/ -v
=> 18 passed
```

覆盖范围：评估器、缓存、批量、粘合流水线、报告构建、阶段 6 验收（四条 query + 二次缓存命中 + 批量顺序）。

### 4.2 报告汇总指标（summary 对照）

| 指标 | 第一轮（冷缓存） | 第二轮（热缓存） | 对照解读 |
|------|------------------|------------------|----------|
| 批量返回条数 | 4/4 | 4/4 | 批量链路完整，无失败项 |
| 缓存命中率 | 0.0（0/4） | **1.0（4/4）** | 相同输入二次执行全部命中缓存 |
| 平均耗时（秒） | — | 0.0041 | 命中后几乎不再触发生成调用 |
| rouge1 均值 | 0.0768 | 0.0768 | 两轮评估结果一致，说明缓存复用稳定 |
| key_info_recall 均值 | 0.2321 | 0.2321 | 同上 |
| hallucination_risk 均值 | 0.0 | 0.0 | 本批答案未触发绝对化风险信号 |
| 失败样本 | 0 | 0 | `failures_*` 均为空列表 |

对应 `summary` 原始片段：

```json
{
  "cache_first_pass": { "hits": 0, "misses": 4, "hit_rate": 0.0 },
  "cache_second_pass": { "hits": 4, "misses": 0, "hit_rate": 1.0 },
  "evaluation_first_pass": {
    "rouge1_avg": 0.0768,
    "key_info_recall_avg": 0.2321,
    "hallucination_risk_avg": 0.0
  }
}
```

### 4.3 单条 query 指标对照（first_pass）

| # | query | rouge1 | key_info_recall | matched | missing（节选） | cache.hit | latency(s) |
|---|-------|--------|-----------------|---------|-----------------|-----------|------------|
| 1 | What is the treatment for MI? | 0.0720 | 0.0000 | — | reperfusion, statin, beta blocker... | false | 0.0032 |
| 2 | metformin cardiovascular effects | 0.1240 | 0.0000 | — | cardiovascular benefit, study design... | false | 0.0034 |
| 3 | papers on malaria after 2015 | 0.0407 | **0.5000** | malaria, after 2015, intervention | published, outcomes, safety | false | 0.0051 |
| 4 | warfarin atrial fibrillation elderly | 0.0704 | **0.4286** | elderly, atrial fibrillation, warfarin | INR monitoring, bleeding risk... | false | 0.0037 |

说明：

- **ROUGE / recall 偏低与样本库强相关**（见 §1.2）：08 快照基于 06 **样本库**（1,267 chunks）上的离线检索结果生成；`ground_truth.json` 为临床指南型参考，二者本就不在同一证据基础上对比。
- 08 流水线在证据不足时会明确写出 uncertainty（如 Q1 自述 “context does not provide specific treatment”），与 `ground_truth` 中 reperfusion / statin 等治疗要点自然重叠有限——这反映**检索覆盖 + 答案保守策略**，不单是评估器问题。
- Q1 额外因素：`pipeline_eval.json` 无 `What is the treatment for MI?` 精确条目，08 使用 fallback reranked，弱相关 chunk 进一步拉低 recall。
- key_info_recall 在 Q3/Q4 能检出部分关键短语，说明评估维度在样本库范围内仍有区分度。
- 第一轮 `cache.hit=false` 符合冷启动预期。

### 4.4 输出结构示例（单条 result 对照）

以 Q4（warfarin）第一轮结果为例，统一输出结构如下：

```json
{
  "query": "warfarin atrial fibrillation elderly",
  "generation": {
    "answer": "The context provided focuses on the detection of atrial fibrillation (AF) in elderly patients...",
    "sources": [{"doc_id": "PMC509245", "source_title": "SAFE trial ..."}],
    "model_name": "deepseek-r1:7b",
    "provider": "stage08_snapshot"
  },
  "evaluation": {
    "rouge": {"rouge1": 0.0704, "rouge2": 0.0295, "rougeL": 0.0469},
    "key_info_recall": 0.4286,
    "key_info_matched": ["elderly", "atrial fibrillation", "warfarin"],
    "key_info_missing": ["stroke risk", "inr monitoring", "bleeding risk"],
    "hallucination_risk": 0.0,
    "readability": {"num_sentences": 12.0, "avg_sentence_len_words": 25.8333}
  },
  "cache": {
    "hit": false,
    "key": "395d7223906df66cb3c0c13c00159ad57099a9ef90940864332555d15b75e8d9",
    "stats": {"hits": 0, "misses": 4, "size": 4}
  }
}
```

第二轮同 query 的关键差异（缓存层）：

```json
{
  "query": "warfarin atrial fibrillation elderly",
  "evaluation": { "rouge": {"rouge1": 0.0704}, "key_info_recall": 0.4286 },
  "cache": { "hit": true, "stats": {"hits": 4, "misses": 4, "size": 4} },
  "latency_seconds": 0.0030
}
```

可见：评估指标保持一致，但 `cache.hit` 从 `false` 变为 `true`，耗时维持毫秒级。

### 4.5 批量与失败样本对照

`batch_stats` 原始输出：

```json
{
  "total": 4,
  "succeeded": 4,
  "failed": 0,
  "avg_latency_seconds": 0.0041,
  "error_rate": 0.0
}
```

失败样本字段：

```json
{
  "failures_first_pass": [],
  "failures_second_pass": []
}
```

说明批量执行未出现单条拖垮整批的情况，且输出顺序与输入 query 列表一致（`_index` 0→3）。

### 4.6 结论

- **评估链路有效**：四条 query 均产出完整 `evaluation` 结构，不是只有汇总均值。
- **缓存链路有效**：第二轮 4/4 命中，验证了“少重复写”目标。
- **批量链路有效**：4/4 成功、0 失败、顺序对齐。
- **指标解释边界**：offline 快照来自样本库检索链，与人工 `ground_truth` 存在证据基础差距；应并列查看 ROUGE 与 key_info_recall。全量 live 对照见 **§6**。

---

## 5. 局限与后续扩展

> 与 `schedule.md`「暂不实施项（占位符与扩展抓手）」对齐。下列五项本阶段**未完整实现**，但已在代码中预留接口或配置键；设计权衡详见 `笔记/09笔记.md` Q4。

### 5.0 速览

| 扩展项 | 配置键 / 接口 | 本阶段状态 |
|--------|---------------|------------|
| 持久化缓存后端 | `config.cache_backend`、`BaseCacheBackend` | 进程内 LRU+TTL 已验证；持久化未接入 |
| 引用联动幻觉降权 | `link_signals_with_sources()`、`hallucination_weight_profile` | 规则计分已通；`sources` 降权未实现 |
| 语义级关键信息匹配 | `semantic_key_info_match`（待增） | 子串 recall 已通；语义匹配未实现 |
| 自适应 TTL | `config.ttl_policy` | 固定 24h 已通；按类型动态 TTL 未实现 |
| 自适应并发调参 | `BatchRunner.summarize()` → `max_workers` | 静态并发已通；动态闭环未实现 |
| 全量语料复评 | `full_corpus_eval_validation` / notebook 阶段 7 / CLI | **已完成**（§6）；`--mode live --retrieval-mode full` |

---

### 5.1 持久化缓存后端

`config.cache_backend`（默认 `memory`；后续可考虑 `sqlite` / `redis`）

**当前实现**

- `GenerationCache` 以进程内 `OrderedDict`（`_index`）承载 LRU/TTL。
- `make_key()` 对 `query + context_text + model + temperature_bucket`（保留 2 位小数）做 `sha256`。
- `set()` 受 `max_temperature=0.3` 门控，TTL 取 `config.ttl_seconds=86400`（24h）。
- `BaseCacheBackend` / `MemoryCacheBackend` 已定义，但 **`config.cache_backend` 未被读取**；实际读写均走 `_index`（仅 `delete()` 会调用 backend）。

**实测验证**

- `eval_cache_batch_report.json`（offline）：首轮 `hit_rate=0.0`（4 miss），第二轮 `hit_rate=1.0`（4 hit）。
- 进程重启后缓存清空；`force_refresh=True` 可绕过命中（`test_pipeline_with_eval.py` 已覆盖）。

**扩展落点**

- 在 `GenerationCache.__init__` 按 `cache_backend` 选择后端，将 `get/set` 从 `_index` 下沉到 backend。
- 持久化优先考虑 `sqlite`（单机）或 `redis`（多实例共享）。

**设计权衡**（Q4-1、Q4-4）

- 缓存命中判断「输入场景是否相同」（query + context + model + temperature），与 ROUGE 等输出质量指标无关——两者并列，一个管性能、一个管质量。
- MVP 进程内 LRU+TTL 已验证重复调用收益；进程退出即失效在开发期可接受。
- 上线或多实例、需跨重启复用时再升级持久化；TTL 开发期 24h，稳定后可配 24h~7d，医学内容宜保守并保留 `force_refresh`。

---

### 5.2 引用联动幻觉降权

`evidence_linked_hallucination_scoring`；当前按 `patterns.py` 规则计分，后续加「有引用降权」

**当前实现**

- `detect_hallucination_signals()` 匹配 4 类正则（`studies show`、`has been proven`、`100%`、绝对安全/有效表述），权重写死在 `HALLUCINATION_SIGNAL_PATTERNS`。
- `evaluate()` 已将 `generation.sources` 传入 `link_signals_with_sources()`，但该函数**当前透传**（`sources` 未参与计算）。
- `config.hallucination_weight_profile` 已声明，**未被读取**。模块 docstring 明确：幻觉分为风险信号，非真伪裁定。

**实测验证**

- offline 四条快照：`hallucination_risk_avg=0.0`，`hallucination_signals` 均为 `[]`——快照未触发规则，**尚无法从产出验证降权逻辑**。
- 单测 `test_detect_hallucination_signals` 对含 `100%` 样例可检出信号。

**扩展落点**

- 在 `link_signals_with_sources()` 实现「有 `sources` 时按 `hallucination_weight_profile` 降权」。
- 权重配置从 `patterns.py` 迁移到 `config` 或 profile 文件。

**设计权衡**（Q4-3）

- 绝对化表述规则定位为**风险筛查**（`risk signal`），不是真伪终审。
- 若原论文本身表述绝对、模型如实复述，规则仍可能标高风险——「宁可保守提醒」的可预期误报。
- 降误报需联合 08 `sources`：有明确引用时降低严重等级（绝对词+无引用权重高，有来源权重低），而非直接判错。

---

### 5.3 语义级关键信息匹配

`semantic_key_info_match`；当前 `key_phrases` 子串匹配，后续可考虑 embedding 语义匹配

**当前实现**

- `key_info_recall()` 对 `ground_truth.json` 的 `key_phrases` 做规范化（小写、压缩空白）后做**子串包含**判断，`recall = matched / len(gt_phrases)`。
- `extract_key_info()`（正则+关键词）已实现但 **未参与 recall 计算**。
- ROUGE 由 `rouge_score.RougeScorer` 独立输出，与 recall 无耦合。

**实测验证**

- offline 首轮：`rouge1_avg=0.0768`，`key_info_recall_avg=0.2321`（见 §4.2、§4.3）。
- 逐条 recall：MI=0.0（7 短语全漏）、metformin=0.0、malaria=0.5、warfarin=0.4286。
- 同义改写或词形变化（如 `statins` vs `statin`）当前**不会**计为命中。

**扩展落点**

- 新增 `semantic_recall` 字段（embedding 相似度阈值），与现有子串 recall 并列输出，不替换现有指标。

**设计权衡**（Q4-2）

- 任务书 `recall = overlap / gt_matches` 的工程落地是「`key_phrases` 短语重叠」，能判断关键点是否被覆盖，**不能**证明模型从正确证据链推导。
- 09 评估是自动近似，非医学事实终审；更稳妥应联合 08 `sources`、key_info_recall、幻觉风险及人工 spot check。
- ROUGE 对结构化答案可能偏低，故与 recall 并列展示；语义匹配作为补充而非替代。

---

### 5.4 自适应 TTL 策略

`config.ttl_policy`（当前 `"fixed"`）；后续按 query 类型动态 TTL

**当前实现**

- `GenerationCache.set()` 统一使用 `self.ttl_seconds`（默认 86400）；**`config.ttl_policy` 未被读取**。
- 强制刷新由 `run_with_cache_and_eval(force_refresh=True)` 实现，跳过 `get` 直接重算。

**实测验证**

- 单测 `test_ttl_expiration_returns_none` 验证过期后 `get` 返回 `None`。
- CLI/notebook 两轮同参复跑第二轮全命中；**未覆盖**按 query 类型差异化 TTL。

**扩展落点**

- 在 `set()` 入口根据 `ttl_policy` 与 query 元数据（如 `ground_truth.json` 标签）选择 TTL；保留 `force_refresh`。

**设计权衡**（Q4-4）

- 固定 TTL 满足 MVP「可控时效 + 可复现实验」。
- 不同 query 时效敏感度不同（治疗指南 vs 文献检索），动态策略可在复用收益与知识过时风险间折中。
- 医学内容建议偏保守，并始终保留 `force_refresh=True`，避免错误结论被长期复用。

---

### 5.5 自适应并发调参

`auto_tune_max_workers`；当前静态 `max_workers`，后续按失败率/延迟动态调整

**当前实现**

- `BatchRunner.__init__` 取 `min(config.max_workers, min(4, os.cpu_count() or 1))`，默认上限 4。
- `run_batch()` 用 `ThreadPoolExecutor` 并行多 query；`summarize()` 输出 `avg_latency_seconds`、`error_rate`，**但不回写** `max_workers`。
- 并行边界：多 query 并行，单 query 内部 08 阶段仍串行。

**实测验证**

- `eval_cache_batch_report.json`：`max_workers=2`，`error_rate=0.0`，`avg_latency_seconds=0.0041`（offline 快照，无真实 LLM 等待）。
- live 模式下 Q1 `generation_metrics.total_time_seconds≈33.4`，瓶颈在推理而非 CPU。

**扩展落点**

- 在 CLI 或 `BatchRunner` 外层增加「上一轮 `summarize()` → 调整下一轮 `max_workers`」闭环（如 `error_rate > 0` 或 P95 延迟超阈值则降并发）。

**设计权衡**（Q4-5）

- 并行发生在**多 query 批处理层**，单 query 内 08 四阶段仍串行——不是「每个 stage 并行再跑一次 LLM」。
- `max_workers = min(4, cpu_count)` 是保守起点；真实瓶颈常在 Ollama 推理与 I/O，非纯 CPU。
- Ollama 排队/超时时宜降至 2，稳定可试 3~4；需结合实测延迟与失败率动态调。

---

### 5.6 已落地、不在上述五项内的能力

| 能力 | 说明 |
|------|------|
| `ModelAdapter` | 已实现 `PipelineModelAdapter` / `SnapshotModelAdapter`，调度层与执行层解耦；多 provider 扩展走适配器而非重写批量框架 |
| `force_refresh` | 已实现，可强制绕过缓存重算 |
| 进程内缓存 MVP | 第二轮 4/4 命中，满足本阶段「少重复写」验证目标 |

**详细占位符与代码路径对照**：见 `schedule.md` §「暂不实施项」；**任务书细节 Q&A**：见 `笔记/09笔记.md` Q4。

---

### 5.7 全量语料复评 ✅

> **2026-07-08 已完成**（阶段 7 · `answer-eval-cache-batch-full.ipynb` C0–C7）。实测表、检索/BM25 对照、耗时解读见 **§6**；细节 Q&A 见 `笔记/09笔记.md` Q5/Q6。

---

## 6. 全量语料 live 复评（2026-07-08）

> **数据来源**：`outputs/samples/eval_cache_batch_report_full.json`（notebook **C5** 两轮 live 评估）  
> **对照基线**：§4 offline 样本库报告 `eval_cache_batch_report.json`  
> **Notebook**：`notebooks/answer-eval-cache-batch-full.ipynb`（C0–C7）

### 6.1 验证范围（与 §1.2 样本库对比）

| 维度 | 阶段 0–6（§4） | 阶段 7（本节） |
|------|----------------|----------------|
| 检索语料 | 1,267 chunks（`mode=sample`） | **6,107,296** chunks（`mode=full`） |
| 检索方式 | 06 `pipeline_eval.json` 离线快照 | `RetrievalPipeline.from_mode("full")` **live 全量检索** |
| BM25 | 样本库现场 build | **分片离线索引** `09/data/bm25_full`（62 片） |
| 生成 | 08 快照（offline）或样本 live | Ollama **live**（`deepseek-r1:7b`） |
| 单条耗时 | ~0.003 s（跳检索） | 首轮 **487–517 s**；二轮 cache hit **193–218 s** |

**数据链路**：

```text
09/data/oa_comm_chunks.jsonl + 04 chroma_db_full + 09/data/bm25_full
  → RetrievalPipeline.from_mode("full")（向量 + 分片 BM25 + RRF + rerank）
  → MedicalGenerationPipeline.run()（Ollama live）
  → PipelineWithEval（GenerationCache + AnswerEvaluator）
  → eval_cache_batch_report_full.json
  （notebook C5 或 CLI: run_eval_cache_batch.py --mode live --retrieval-mode full）
```

### 6.2 前置：全量 BM25 分片索引

| 项 | 实测 |
|----|------|
| 构建入口 | notebook C2.5 / `scripts/build_bm25_full_index.py` |
| 落盘路径 | `09/data/bm25_full/`（`manifest.format=bm25_sharded_v1`） |
| 规模 | **62 片** · **6,107,296** chunks · `shard_size=100000` |
| 构建耗时 | **700.7 s**（~11.7 min） |
| 加载方式 | `MultiPathRetriever.from_mode("full")` 自动 `ShardedBM25Index` |

分片近似验证（C4.5，`bm25_sharded_vs_mono_overlap.json`）：

| 对照 | avg_overlap_rate | 解读 |
|------|------------------|------|
| 同语料（样本库分片 vs 单体） | **0.95** | 分片算法对 keyword top-10 影响极小 |
| 全量分片 vs 样本单体 | 0.0 | 语料规模不同，预期现象 |

### 6.3 报告汇总指标（样本 vs 全量）

数据来源：`outputs/samples/eval_sample_vs_full.json`

| 指标 | 样本 offline（§4） | 全量 live（首轮） | Δ |
|------|---------------------|-------------------|---|
| rouge1_avg | 0.0768 | **0.0782** | +0.0014 |
| key_info_recall_avg | 0.2321 | **0.2738** | **+0.0417** |
| hallucination_risk_avg | 0.0 | 0.0 | 0 |
| 缓存二轮命中率 | 1.0 | **1.0** | — |
| 单条 latency（首轮） | ~0.003 s | **487–517 s** | — |
| C5 总墙钟（两轮 4+4） | — | **~23 min** | 评估跑法，非生产单问 |

全量 live 两轮缓存（`eval_cache_batch_report_full.json`）：

| 轮次 | cache hit_rate | rouge1_avg | key_info_recall_avg |
|------|----------------|------------|---------------------|
| 首轮（冷缓存） | 0.0（0/4） | 0.0782 | 0.2738 |
| 二轮（热缓存） | **1.0（4/4）** | 0.0782 | 0.2738 |

说明：二轮命中仅跳过 **LLM 生成**；`resolve_context_text()` 仍执行全量检索，故二轮仍有 ~3.5 min/条（详见 `笔记/09笔记.md` Q5）。

### 6.4 单条 query 指标对照

| # | query | 样本 rouge1 | 全量 rouge1 | 样本 recall | 全量 recall | 全量 latency(s) |
|---|-------|-------------|-------------|-------------|-------------|-----------------|
| 1 | What is the treatment for MI? | 0.0720 | 0.0518 | 0.0 | 0.0 | 507.3 |
| 2 | metformin cardiovascular effects | 0.1240 | **0.1311** | 0.0 | **0.3333** | 516.9 |
| 3 | papers on malaria after 2015 | 0.0407 | **0.0675** | 0.5000 | 0.3333 | 487.6 |
| 4 | warfarin atrial fibrillation elderly | 0.0704 | 0.0625 | 0.4286 | 0.4286 | 491.3 |

**解读要点**：

- **全量 recall 均值略升**（+0.042），说明切换 610 万库后部分 query 能覆盖更多 `key_phrases`（如 Q2 metformin 命中 cardiovascular 相关短语）。
- **rouge1 基本持平**，未出现「全量一定更高」的简单规律——ROUGE 对答案长度与表述风格敏感。
- **Q1 MI recall 仍为 0**：全量检索命中 herbal/PRP 等主题文献，与手写 `ground_truth`（再灌注、他汀、β 阻滞剂等指南要点）字面差异大；**recall=0 不代表检索失败**，须结合 `sources` 与人工 spot check。
- **Q3 malaria**：样本 recall 0.5 > 全量 0.333，因全量答案表述与 `key_phrases` 子串匹配规则不完全一致，非检索退化。

### 6.5 检索 Top-1 变化（样本 vs 全量）

数据来源：`outputs/samples/pipeline_eval_sample_vs_full.json`（06 `pipeline_eval.json` vs `pipeline_eval_full.json`）

| query | 样本 Top-1 doc_id | 全量 Top-1 doc_id | 变化 |
|-------|-------------------|-------------------|------|
| MI treatment | — | — | 无 reranked 条目 |
| metformin cardiovascular | PMC520826 | **PMC2566605** | ✅ 变化 |
| malaria after 2015 | PMC523837 | **PMC12822982** | ✅ 变化 |
| warfarin AF elderly | PMC509245 | —（全量快照无 reranked） | 变化 |

与 06 C12 结论一致：切换全量库后 Top-1 候选显著变化，样本库指标**不能外推**为生产检索质量。

### 6.6 结论与边界

| 结论 | 说明 |
|------|------|
| **链路验证** | 4/4 query 全量 live 跑通；`evaluation` 字段完整；生成缓存二轮 100% 命中 |
| **指标可解释性** | 全量 `key_info_recall` 略高于样本；`rouge1` 持平；幻觉风险均为 0 |
| **BM25 分片** | 同语料重叠 0.95，生产可用；全量索引已落盘供后续工程复用 |
| **耗时** | 单条首轮 ~8 min（检索+r1 生成）；C5 两轮 ~23 min 为评估成本，生产单问可更低（见 Q5） |
| **未做项** | `ground_truth` 分集修订、E: 手动备份 |

**核心回答**：阶段 0–6 证明 09 **模块正确**；阶段 7 证明在 **610 万全量检索覆盖** 下指标**可测量、可对照、可解释**——全量并非全面优于样本，但检索证据基础已切换，后续 RAG 质量讨论应以全量链路为准。

---

## 7. 复现入口

### 7.1 阶段 0–6（样本库 offline）

```powershell
conda activate med-rag-verify
cd "D:\谷歌\09 生成答案评估，缓存策略与批量处理"
pytest tests/ -v
python scripts/run_eval_cache_batch.py --mode offline
```

Notebook：`notebooks/answer-eval-cache-batch.ipynb`（C0–C6）。

### 7.2 阶段 7（全量 live）

```powershell
conda activate med-rag-verify
cd "D:\谷歌\09 生成答案评估，缓存策略与批量处理"

# 1) BM25 分片索引（首次；已构建可 --status 查看）
python scripts/build_bm25_full_index.py --status
python scripts/build_bm25_full_index.py --shard-size 100000

# 2) 全量资源检查
python scripts/run_eval_cache_batch.py --mode live --retrieval-mode full --check-only

# 3) 全量 live 评估（等价 notebook C5；4 条 × 两轮缓存，耗时长）
python scripts/run_eval_cache_batch.py --mode live --retrieval-mode full --max-workers 2
# 可选：--skip-rerank（快速 smoke）、--skip-second-pass、--output path\to\report.json

# 或 notebook：answer-eval-cache-batch-full.ipynb（C0–C7）
```

**CLI 参数（全量 live）**：

| 参数 | 默认 | 说明 |
|------|------|------|
| `--mode live` | — | 必须；调用 Ollama |
| `--retrieval-mode full` | `sample` | 610 万 `RetrievalPipeline.from_mode("full")` |
| `--max-workers` | `min(4, cpu)` | 建议 **2**，避免 Ollama 排队 |
| `--check-only` | — | 仅检查 Chroma/BM25 语料等资源 |
| `--output` | `eval_cache_batch_report_full.json` | 报告输出路径 |

**产出 JSON**：

| 文件 | 说明 |
|------|------|
| `outputs/samples/eval_cache_batch_report_full.json` | 全量 live 评估报告 |
| `outputs/samples/eval_sample_vs_full.json` | 样本/全量指标对照 |
| `outputs/samples/pipeline_eval_sample_vs_full.json` | 检索 Top-1 对照 |
| `outputs/samples/bm25_sharded_vs_mono_overlap.json` | 分片 BM25 重叠率 |
| `data/bm25_full/manifest.json` | BM25 索引元数据（不进 Git） |
