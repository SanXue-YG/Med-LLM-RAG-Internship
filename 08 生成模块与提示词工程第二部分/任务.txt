生成模块与提示词工程第二部分：

1 本地LLM集成与生成

class LLMGenerator:
	
	    Args:
            model_name: Ollama模型名称
            base_url: Ollama服务地址
            timeout: 请求超时时间（秒）

	#初始化并测试链接
		#温度和token数
	
	#生成方法
		# 使用prompt，（+system prompt），temperature，max_tokens构建完整提示
		# 根据模型格式添加系统提示，并添加Json格式要求
		# 从文本中提取json部分，并编辑格式（添加缺失符号）
		
	#批量生成
	

2 整合所有组件，完成生成流程

class MedicalGenerationPipeline:
	
	#初始化组件（上下文，医学模板，LLM生成）
	
	# 1. 上下文组装
	# 2. 检索结果评估（可选）
		# 填充提示模板并生成评估
		# 使用评估结果筛选上下文
			基于文档id筛选，检测文档标题行等
	# 3. 提取评估结果并生成答案草稿
	# 4. 批判性审查（可选）
	# 5. 生成最终答案
		如果成功审查，使用审查结果
		如果没有审查，直接使用草稿
	# 6. 生成后处理
		添加引用标记，添加格式美化，免责声明
	# 组装最终结果
	result = {
            "query": 
            "answer": 
            "context_metadata": context_result["metadata"],
            "generation_metrics": {
                "total_time_seconds": 
                "stage_times": 
                "token_counts": 
                "stage_success": 
            },
            "intermediate_results": {
                "evidence_evaluation": 
                "draft_answer": 
                "review_feedback": 
            },
            "sources": self._format_sources(context_result["selected_chunks"]),
            "timestamp": time.strftime("%Y-%m-%d %H:%M:%S")
        }
	
	# 计算总时间

3. 测试完整流程
	编写测试query
	完善答案显示和统计信息，log测试query结果以及相关引用来源
	log每次生成的关键指标
		耗时，答案长度，阶段