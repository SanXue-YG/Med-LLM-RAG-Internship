# Med-RAG 流程图

```text
┌────────────┐     HTTP      ┌──────────────────────────────┐
│  React UI  │ ────────────► │  FastAPI (backend/app)       │
│  Vite:5173 │ ◄──────────── │  /qa /sessions /ingest ...   │
└────────────┘               └──────────────┬───────────────┘
                                            │
                     ┌──────────────────────┼──────────────────────┐
                     ▼                      ▼                      ▼
              data/chat/            ConstrainedGeneration    data/raw_uploads
           (会话 JSON)              Pipeline (stage10)       → ingest 02–04
                                            │
                     ┌──────────────────────┼──────────────────────┐
                     ▼                      ▼                      ▼
              stage05 查询增强        stage06 检索融合重排     stage07/08 组装生成
                     │                      │                      │
                     └──────────► data/chroma + chunks (+ Ollama) ◄─┘
```

## 语料更新（回形针）

```text
上传 XML/JSONL
  → data/raw_uploads/
  → parse / chunk
  → append chunks_sample.jsonl
  → Chroma collection.add (sample)
  → documents_sample.sqlite upsert
  → reset RagService singleton
```
