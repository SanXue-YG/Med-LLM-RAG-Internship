# 09 生成答案评估，缓存策略与批量处理 — Q&A 笔记

> **用途**：个人知识点 Q&A（非交付文档）  
> 记录时间：2026-07-07  
> 对应任务：`09 生成答案评估，缓存策略与批量处理/任务.txt`  
> 关联：08 `MedicalGenerationPipeline` / `generation_eval.json`、07 `ContextAssembler`、06 `RetrievalPipeline`、[`schedule.md`](../09%20生成答案评估，缓存策略与批量处理/schedule.md)

---

## Q1：第九阶段在整个 RAG 系统中做什么？处于什么位置？

### 一句话

**在 08 已经能「问得出答案」的基础上，给答案加上「质量体检仪」、给生成链路加上「记忆与加速层」**——本周是端到端 RAG 闭环的**质量保障与工程优化层**：不重建检索、不重写 Prompt，而是包裹在 `MedicalGenerationPipeline.run()` 外侧，做评估、缓存与批量调度。

### RAG 全流程与本阶段位置

```text
┌─────────────────────────────────────────────────────────────────────┐
│                        RAG 系统全流程                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  【离线建库】（02–04，已完成）                                        │
│  原始 XML → 清洗 → 分块 → 向量化 → ChromaDB（610 万 chunks）         │
│                                                                      │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                      │
│  【在线检索链】（05 + 06，已完成）                                    │
│                                                                      │
│  用户 query → 05 增强 → 06 多路召回 → 融合 → 重排 → Top-K chunks     │
│                                                                      │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                      │
│  【生成准备层】（07，已完成）                                         │
│                                                                      │
│  Top-K → ContextAssembler → context_text + selected_chunks           │
│           PromptStage 四阶段模板（仅渲染，不调 LLM）                  │
│                                                                      │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                      │
│  【生成执行层】（08，已完成）                                         │
│                                                                      │
│  MedicalGenerationPipeline.run(query)                                │
│      → 检索 → 组装 → 多步 LLM → 后处理 → result { answer, sources }  │
│                                                                      │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                      │
│  ★ 09 本阶段重点 ★  【质量保障 + 工程优化层】                        │
│                                                                      │
│  query (+ 可选 force_refresh)                                      │
│      │                                                               │
│      ├─ GenerationCache 查键 hash(query, context, model, temp)     │
│      │     ├─ HIT  → 直接返回缓存答案（跳过 LLM）                     │
│      │     └─ MISS → 调用 08 pipeline → 低温成功则写入缓存          │
│      │                                                               │
│      ├─ AnswerEvaluator.evaluate(answer, ground_truth)               │
│      │     ├─ ROUGE（文本相似性）                                    │
│      │     ├─ key_info_recall（医学关键信息召回）                    │
│      │     ├─ hallucination_risk（绝对化 / 无引用信号）              │
│      │     └─ readability（句长、句数等）                            │
│      │                                                               │
│      └─ BatchRunner（optional）多 query 并行调度                     │
│            → ThreadPoolExecutor，顺序与输入一致                       │
│      │                                                               │
│      ▼                                                               │
│  扩展 result { generation, evaluation, cache }                       │
│                                                                      │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                      │
│  【后续 LangChain_RAG（待定）】  LCEL 封装、流式 UI、分布式缓存       │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 各阶段分工对照

| 阶段 | 在 RAG 里扮演什么 | 本阶段**不做**什么 |
|------|------------------|-------------------|
| 01 | 验证本地 LLM + 数据源可行 | — |
| 02–04 | 离线：数据清洗、分块、建向量库 | 不处理用户 query |
| 05 | 理解 query，产出 `EnhancedQuery` | 不做检索、不做生成 |
| 06 | 在线检索：召回 → 融合 → 重排 | 不做上下文组装、不做 LLM 生成 |
| 07 | 生成准备：组装 context + 医学 Prompt 模板 | **不调用** LLM |
| 08 | 生成执行：Ollama 集成 + 多步流水线 + 后处理 | **不实现** 答案质量评分、缓存、批量 |
| **09（本周）** | **质量评估 + 缓存 + 批量**：包裹 08 输出，量化好坏、避免重复生成、加速多 query | **不实现** 人工标注平台、在线 A/B、Redis 分布式缓存 |
| LangChain RAG（待定） | 产品化封装与交互层 | — |

### 与第八阶段的衔接（08 出答案，09 评答案、记答案、批处理答案）

| 维度 | 08（已完成） | 09（本周） |
|------|-------------|-----------|
| 核心问题 | **答得出**：端到端生成 `answer` + `sources` | **答得好不好、要不要重算、能不能批量跑** |
| 主要产出 | `MedicalGenerationPipeline`、`generation_eval.json` | `AnswerEvaluator`、`GenerationCache`、`BatchRunner` |
| LLM 调用 | 每条 query 默认走 08 多步生成（可理解为一条问题要走完整生成链） | 先查缓存：命中时**零次** LLM；未命中才委托 08 生成 |
| 质量度量 | `generation_metrics`（耗时、stage 成功率） | `EvaluationResult`（ROUGE、关键信息 recall、幻觉风险、可读性） |
| 批量 | `run_generation_eval.py` **顺序**跑 4 条（Q1 跑完再 Q2） | `BatchRunner` **并行**调度（optional）：Q1~Q4 可同时开工，但每条内部仍按顺序 |
| 参考答案 | 无 ground truth，只有生成快照 | `ground_truth.json` 供 ROUGE / recall 对比 |

**记忆口诀**：06 负责 **找**；07 负责 **挑、剪、装盒**；08 负责 **问模型、拿答案、贴引用**；**09 负责 **量质量、记结果、批跑加速**；LangChain 负责 **包一层好用的壳**。

### 本周任务书三大块

| 任务书条目 | 通俗理解 | 规划产出 |
|-----------|---------|---------|
| **1. 多维度评估** | 给生成答案做「体检」：和参考答案像不像、关键医学信息有没有漏、有没有绝对化幻觉、好不好读 | `answer_evaluator.py`、`patterns.py` |
| **2. 缓存策略** | 相同 query + 相同 context 不重复调 LLM；控内存、设 TTL、只缓存低温确定性结果 | `generation_cache.py` |
| **3. 批量处理（optional）** | 多条 query 并行跑，单条失败不拖垮整批，输出顺序与输入一致 | `batch_runner.py` |
| **复跑验证** | 用 08 四条固定 query 再跑一遍，证明评估 / 缓存 / 批量都工作 | notebook + `run_eval_cache_batch.py` |

### 端到端数据流（09 视角）

```text
用户 query（英文）
    ↓ cache lookup(key = hash(query, context_text, model, temperature))
    ├─ HIT  → 取缓存 generation result
    └─ MISS → 08 MedicalGenerationPipeline.run(query)
              ↓ 若 temperature ≤ 阈值且成功 → cache.set(...)
    ↓ 09 AnswerEvaluator.evaluate(generated_answer, ground_truth_entry)
              ↓ rouge / key_info_recall / hallucination_risk / readability
    ↓ 汇总 { generation, evaluation, cache: { hit, key, stats } }

批量路径：
list[query] → BatchRunner.run_batch()
    → 每任务走上述 cache + pipeline + evaluate
    → list[result]（顺序与输入 queries 对齐）
```

### 一图看懂：串行和并行到底在说什么

```text
单条 query（内部流程）：
Q1
  └─ cache lookup
      ├─ HIT  -> 直接返回
      └─ MISS -> 08 生成链（按顺序）-> 评估 -> 返回

多条 query（批量流程）：
顺序模式（08脚本默认）：
Q1 -> Q2 -> Q3 -> Q4

并行模式（09 BatchRunner 可选）：
Q1 ----\
Q2 -----+--> 同时执行（每条内部仍是“先cache，再决定是否调LLM，再评估”）
Q3 -----+
Q4 ----/
```

要点：**并行是“多条问题之间并行”，不是“一条问题内部四个阶段并行”。**

### 按当前计划，后续如何落地「查答案 + 少重复写 + 批量更快」

下面对应 `schedule.md` 的阶段 0–5，完成后你会看到很直观的阶段成果。

1. **阶段 0（先把“对照标准”准备好）**  
   先建 `ground_truth.json`（4 条 query 的参考答案 + key phrases），并在 notebook C0 加载校验。  
   结果：后面的“查答案质量”有了统一标尺。

2. **阶段 1（实现“查答案”）**  
   完成 `AnswerEvaluator`，在 notebook C1 演示 ROUGE、关键信息召回、幻觉信号、可读性。  
   结果：每次生成后都能得到一份结构化质量报告，而不是只看主观感觉。

3. **阶段 2（实现“少重复写”）**  
   完成 `GenerationCache`（key=query+context+model+temperature，含 TTL 和低温门控），在 C2/C3 做 miss→hit、过期、高温不缓存演示。  
   结果：同样输入第二次基本不再调 LLM，速度提升且省调用成本。

4. **阶段 3（实现“批量更快”）**  
   完成 `BatchRunner`，用线程池并行跑多条 query；单条失败不影响其他条，并保持输出顺序一致，在 C4 验证。  
   结果：一次跑 4 条评测 query 的总耗时明显下降。

5. **阶段 4（把三件事真正串起来）**  
   完成 `pipeline_with_eval.py`，统一输出 `{generation, evaluation, cache}`，在 C5 跑 08 的四条基准 query 做汇总。  
   结果：形成可直接复用的“生成+评估+缓存”一体化接口。

6. **阶段 5（收尾导出与复现）**  
   C6 导出 `eval_cache_batch_report.json`，CLI 脚本做 notebook 等价复跑。  
   结果：有可提交的样例报告与可重复执行入口。

### 关键认知

1. **09 不改变 RAG 的「检索增强」核心路径**——检索、组装、Prompt、LLM 生成仍由 05–08 负责；09 是在 `run()` **外侧**加评估器、缓存器和批量调度器，属于**横切关注点**（cross-cutting concern）。
2. **评估的是「生成答案」，不是「检索质量」**——06 的 `pipeline_eval.json` 评的是 Top-K 命中；09 评的是 08 产出的 `answer` 文本是否忠实、完整、安全。两者互补，不可混用。
3. **缓存键必须含 `context_text`**——同一 query 若检索结果变化（索引更新、mode 切换 sample↔full），context 不同则不应命中旧缓存；schedule 已把 `model_name` 和 `temperature` 纳入键，设计合理。
4. **幻觉检测是启发式规则，不是事实核查**——任务书的「研究表明」「100%」「完全安全」等信号，与 08 `critical_reviewer` 的 LLM 审查互补：09 提供**可重复、零成本**的规则分，便于批量对比与回归测试。
5. **ground truth 需人工整理**——08 `generation_eval.json` 只有生成快照，没有标准答案；本周要先为 4 条 query 手写 `reference_answer` + `key_phrases`，ROUGE 与 recall 才有意义。
6. **本阶段边界清晰**——任务书要的是评估器 + 进程内 LRU 缓存 + 可选线程池批量，不是人工标注平台、不是 Redis、不是在线 A/B 实验框架。

### 09 扩展 `result` 在系统里的意义

| 字段 | 谁用 | 作用 |
|------|------|------|
| `generation` | 与 08 兼容 | 原 `MedicalGenerationPipeline.run()` 返回值 |
| `evaluation.rouge` | 评测 / 回归 | 与 `ground_truth.reference_answer` 的文本重叠度 |
| `evaluation.key_info_recall` | 医学场景重点 | 剂量、时间、副作用等关键信息是否覆盖 |
| `evaluation.hallucination_risk` | 安全审计 | 绝对化表述风险分，可设阈值告警 |
| `evaluation.readability` | 用户体验 | 句长、句数等可读性粗指标 |
| `cache.hit` / `cache.stats` | 性能优化 | 二次同 query 是否跳过 LLM；命中率统计 |
| `error`（批量时） | 容错 | 单条失败不中断整批，便于大批量评测 |

### 与 08 固定测试 query 的对应

| # | query（08 `DEFAULT_QUERIES`） | 09 验证侧重 |
|---|------------------------------|------------|
| 1 | `What is the treatment for MI?` | ROUGE；治疗建议类 key phrase recall |
| 2 | `metformin cardiovascular effects` | 剂量 / 机制类正则提取 |
| 3 | `papers on malaria after 2015` | 时间范围正则；年份相关 gt |
| 4 | `warfarin atrial fibrillation elderly` | 安全信息（bleeding risk）与幻觉信号 |

---

## Q2：当前 `schedule.md` 计划是否合理？（启动前审阅）

> 以下为 2026-07-07 对照任务书与 08 产出的审阅结论，供决定是否微调计划。

### 总体判断：**合理，可直接按 schedule 执行**

任务书三大块（评估、缓存、批量 optional）与 schedule 阶段 0–6 一一对应；上游接口（08 `MedicalGenerationPipeline`、`generation_eval.json` 四条 query；07 `context_text` 作缓存键材料）已就绪，无结构性缺口。

### 与任务书对齐情况

| 任务书要求 | schedule 是否覆盖 | 备注 |
|-----------|------------------|------|
| ROUGE 文本相似性 | ✅ 阶段 1 `score_rouge` | `rouge-score` 库 |
| 关键信息提取 + recall | ✅ 阶段 1 `patterns.py` + `key_info_recall` | 正则 + `ground_truth.key_phrases` |
| 幻觉检测 | ✅ 阶段 1 `detect_hallucination_signals` | 英文信号为主，与语料一致 |
| 可读性（平均句长等） | ✅ 阶段 1 `readability_metrics` | 任务书明确要求 |
| 缓存键 query + context 哈希 | ✅ 阶段 2 `make_key` | 含 model + temperature |
| 大小限制 / TTL / 低温门控 | ✅ 阶段 2 LRU + `max_entries` + `ttl_seconds` + `max_temperature` | 进程内首版足够 |
| 批量 ThreadPoolExecutor（optional） | ✅ 阶段 3 | 单失败不中断、顺序一致 |
| 复跑上周测试 query | ✅ 阶段 5 notebook + CLI | 对齐 08 四条 |

### 建议保留的设计决策（无需改计划）

| 决策 | 理由 |
|------|------|
| `ground_truth.json` 人工整理 4 条 | 08 快照无标准答案；手写 `reference_answer` + `key_phrases` 是 ROUGE/recall 的前提 |
| 进程内 LRU，首版不落盘 Redis | 任务书强调防 OOM；与实习工程规模匹配 |
| `temperature <= 0.3` 才缓存 | 对齐任务书「只缓存低温度确定性结果」 |
| TTL 24h～7d 可配 | 医学知识有时效性，不永久缓存 |
| `max_workers = min(4, cpu_count)` | Ollama 单模型并发过高易排队超时 |
| `pipeline_with_eval.py` 粘合层 | 避免改动 08 源码，与 05–07 引用上游 `src` 的方式一致 |
| mock 单测不依赖真实 Ollama | 与 08 pytest 策略一致 |

### 可留意的风险（schedule 已写，执行时记住即可）

| 风险 | 对计划的影响 | 是否需改 schedule |
|------|-------------|------------------|
| ground truth 质量参差 | ROUGE / recall 指标难解释 | ❌ 先 4 条手写，报告注明局限性 |
| ROUGE 对结构化医学答案偏低 | 指标「不好看」 | ❌ 报告并列展示 key_info_recall + 幻觉分 |
| Ollama 并发 4 路超时 | 批量失败率高 | ❌ 可降 `max_workers` 或加串行开关 |
| 缓存命中旧医学结论 | 错误答案被复用 | ❌ TTL + `force_refresh=True` 已规划 |
| 幻觉规则误报（如正当引用场景） | 风险分偏高 | ❌ 权重可配；可与 08 `critical_reviewer` 交叉参考 |
| 任务书写「ROGUE」 | 笔误 | ❌ 实现用 `rouge-score`，schedule 已纠正 |

### 可选微调（非必须，按实现感受再定）

1. **阶段 0 优先完成 `ground_truth.json`**——与 schedule「本周执行顺序」一致，可和 evaluator 并行起草，但测试前必须定稿。
2. **notebook C2 缓存演示**：第二次同 query 连跑应看到 `cache.hit == True` 且 `generation_metrics` 缺失或显著缩短——直观展示价值。
3. **与 08 `critical_reviewer` 的关系**：09 幻觉规则是**离线规则分**，不替代 08 在线 LLM 审查；报告里写清分工即可。
4. **正式报告**：阶段 6 可选 `docs/答案评估与缓存报告.md`，与 08 交付风格对齐。

### 开发与备份工作流（延续 08）

- **单一 notebook 贯穿**：`answer-eval-cache-batch.ipynb` 从阶段 0 创建，每阶段追加 C* 章节。
- **阶段收尾必做**：完成一阶段 → 勾选 `schedule.md` → 更新根目录 `README.md` → git 提交，再进入下一阶段。

### 与 README「LangChain_RAG 待定」的关系

09 完成后，RAG 闭环在**功能上**具备：生成（08）+ 质量评估 + 缓存加速 + 批量评测；**工程上**仍缺 LangChain 的链式抽象、流式 UI、分布式缓存。09 的进程内 LRU 是有意为之的**最小可行缓存**，不应膨胀去替 Redis / 标注平台干活。

---

## Q3：和 08 笔记 Q1 / 08 `result` 的对应关系（复习用）

```text
08 笔记：08 是「生成执行层」，产出 answer + sources
09 本周：在 answer 外侧加 evaluate + cache + batch

08 generation_metrics（耗时）  +  09 evaluation（质量）  =  完整单次 run 画像
08 run_generation_eval（顺序）  →  09 BatchRunner（并行 + 缓存 + 评估）
```

| 08 概念 | 09 落地 |
|---------|--------|
| `MedicalGenerationPipeline.run(query)` | `pipeline_with_eval.run_with_cache_and_eval()` 内部调用 |
| `result["answer"]` | `AnswerEvaluator.evaluate()` 的 `generated` 输入 |
| `assembled.context_text` | `GenerationCache.make_key()` 的材料之一 |
| `generation_eval.json` 四条 query | `ground_truth.json` + 复跑验证基准 |
| `run_generation_eval.py` | 升级为 `run_eval_cache_batch.py`（加评估与缓存统计） |

---

## Q4：任务书细节疑问（2026-07-07）

> 本节整合你对 `任务.txt` 的 5 个关键问题，给出当前阶段可执行的统一口径。

### Q4-1：`ROGUE` 是什么？如何评估文本相似性？它和缓存有什么关系？

**先纠正术语**：任务书里写的 `ROGUE` 基本可以视为 **`ROUGE`** 的笔误。  
`ROUGE`（Recall-Oriented Understudy for Gisting Evaluation）是常见的文本重叠指标，用来比较“生成答案”与“标准答案”的相似度。

**在本周怎么用**（按计划）：

- 用 `rouge-1 / rouge-2 / rouge-L`（通常看 F1）。
- 输入：`generated_answer` vs `ground_truth.reference_answer`。
- 含义：看关键词、短语、句式结构是否覆盖到标准答案核心内容。

**它的作用**：

1. 给答案质量一个可量化分数（不是只靠主观阅读）。
2. 用于回归对比（改 prompt/参数后是否变好）。
3. 作为“查答案”中的一个维度，和 key_info_recall、幻觉风险一起看。

**它和缓存的关系**：

- **不是**直接用来做缓存命中判断。
- 缓存命中看的是“输入是否同一问题场景”（query + context + model + temperature）。
- ROUGE 是“输出质量评估”，缓存是“避免重复计算”。
- 两者在 09 阶段是并列能力：一个管质量，一个管性能。

### Q4-2：如何评估“关键信息是否来自标准答案”？如何保证来源和准确性？

任务书要求的是：`recall = overlap / gt_matches`。  
按当前计划，落地方式是“**标准答案关键短语表 + 生成答案匹配**”。

**具体做法**：

1. 在 `ground_truth.json` 为每条 query 维护：
   - `reference_answer`
   - `key_phrases`（剂量、时间范围、安全信息、机制词等）
2. 对生成答案做正则和短语匹配，得到 `predicted_key_phrases`。
3. 计算与 `key_phrases` 的交集，得到 `key_info_recall`。

**如何判定“来自标准答案”**：

- 工程上是按“短语/模式重叠”判定（即 overlap），不是语义级因果证明。
- 也就是说：它能判断“标准答案中要求的关键点是否被覆盖”，但不能 100% 证明“模型一定从正确证据链推导而来”。

**准确性保障边界（很重要）**：

- 09 的这套是“自动评估近似方法”，不是医学事实终审。
- 更稳妥做法是联合：
  - 08 的 `sources` / 引用结果
  - 09 的 key_info_recall + 幻觉风险
  - 必要时人工 spot check

### Q4-3：`无依据的绝对化表述` 如何判断？如果论文本身就很绝对会不会误报？

当前计划是**启发式风险信号**，不是法庭式真伪裁决。  
例如匹配到“100%”“completely safe”“has been proven”等词，会加风险分。

**如果引用的原论文中明确说明了相关信息但它的表述显得较为绝对，在此处是否会被列为高风险？会。**

- 如果原论文确实这样写，而模型也复述了绝对语句，规则仍可能标高风险（这是可预期的误报）。
- 原因：这套规则是在做“安全提醒”，宁可保守一点，也不直接判真。

**怎么降低误报**（计划内可做）：

1. 把它定义为 `risk signal`，不是 `hallucination verdict`。
2. 与引用信息联动解读：有明确来源时降低严重等级，而不是直接判错。
3. 规则加权：绝对词 + 无引用时权重高；绝对词 + 有来源时权重低。

结论：该指标用于“筛查风险段落”，不是单独定生死。

### Q4-4：缓存怎么实现？存哪里？TTL 多长怎么定？

按当前 schedule，首版是：**进程内 LRU + TTL**（内存缓存）。

**实现形态（本周）**：

- 存储位置：当前 Python 进程内存（不是云端数据库）。
- 生命周期：进程结束缓存失效（除非后续扩展落盘）。
- 键：`hash(query + context_text + model + temperature)`。
- 策略：只缓存低温度、成功生成的结果；超 `max_entries` LRU 淘汰；过期 TTL 删除。

**未来可扩展**：

- 本地持久化：SQLite / JSON / diskcache
- 服务化：Redis（本机或云端）
- 再进一步：多实例共享缓存（线上部署）

**TTL 怎么定**（建议口径）：

- 开发验证期：先用 `24h`（便于复现实验且避免太旧）。
- 稳定后可按任务改成 `24h~7d` 可配置。
- 医学内容建议保守：宁可短一些，并支持 `force_refresh=True` 强制重算。

所以本周是“最小可行缓存（MVP）”，先解决重复调用和可控时效，再决定是否升级为持久化/云端缓存。

### Q4-5：并行发生在哪？单问题四阶段会并行吗？CPU 核心数怎么定？

并行发生在**多 query 批处理层**，不是单 query 内部四阶段。

**正确理解**：

- 单个问题：仍按 08 的流程串行执行（前一步结果给后一步）。
- 多个问题：09 的 `BatchRunner` 可把 Q1/Q2/Q3/Q4 同时发起（可选）。

所以不是“每个 stage 并行再跑一次 LLM”，而是“多个问题各自跑自己的完整链路”。

**`max_workers` 如何按 CPU 调整**（本周建议）：

1. 以保守为主：`max_workers = min(4, os.cpu_count() or 1)`（与 schedule 一致）。
2. 若 Ollama 排队/超时明显：降到 2。
3. 若机器空闲且稳定：可尝试 3~4。

注意：这里虽然提到 CPU 核数，但真实瓶颈常在本地 LLM 推理与 I/O，不是纯 CPU 计算；因此要结合实测延迟和失败率动态调。

---

## Q5：全量 live 评估（阶段 7）为什么跑了 23 分钟？未来 RAG 每条回复也这么慢吗？

> 背景：`answer-eval-cache-batch-full.ipynb` 的 **C5** 在 610 万 chunks 全量链路 + Ollama live 生成下，实测总耗时约 23 分钟。

### 先拆清楚：23 分钟 = 4 条 query × 2 轮评估，不是「单条问答 23 分钟」

C5 是**评估**流程，一次做了 8 次完整任务：

```text
首轮 4 条 query（cache miss，含 LLM 生成）
  +
第二轮 4 条 query（cache hit，测缓存复跑）
  =
共 8 次完整任务
```

实测时间线（与报告 `eval_cache_batch_report_full.json` 一致）：

| 节点 | 时间 | 累计 |
|------|------|------|
| 首轮开始 | 16:54:59 | — |
| 第二轮开始 | 17:11:47 | 首轮 ≈ 17 分钟 |
| 报告写出 | 17:18:39 | 第二轮 ≈ 7 分钟 |
| **合计** | | **≈ 23–24 分钟** ✓ |

### 单条 latency 构成（报告实测）

| 阶段 | 单条 latency | 说明 |
|------|-------------|------|
| 首轮（cache miss） | **487–517 秒（~8 分钟/条）** | 全量检索 + rerank + **deepseek-r1:7b 生成** |
| 二轮（cache hit） | **193–218 秒（~3.5 分钟/条）** | 仍跑全量检索 + rerank + 评估，**跳过 LLM 生成** |

> ⚠️ **关键认知**：当前实现中，即使 cache hit，也会**先跑一遍全量检索**（`resolve_context_text()` 在查缓存之前执行，用于对齐缓存键的 `context_text`）。所以二轮 ~3.5 分钟不是「纯缓存秒回」，检索本身就是第二大瓶颈。

首轮 ~8 分钟/条的耗时大头（粗估）：

1. **deepseek-r1:7b 推理生成** → 推理型模型偏慢，常占 **2–5+ 分钟**（最大瓶颈）
2. **分片 BM25 查询** → 逐片加载 ~61 个 pickle 并算分，**1–3 分钟**（第二瓶颈）
3. **Chroma 向量检索**（610 万）→ 秒级–十几秒
4. **BGE reranker**（CUDA）→ 十几秒–几十秒
5. **ROUGE / recall 评估** → 很快

`MAX_WORKERS=2` 让 4 条 query 两两并行，所以首轮墙钟 ≈ 2×8 ≈ 16 分钟，而非 4×8=32 分钟。

### 未来 RAG 实际使用，每条也这么久吗？——不会

23 分钟是评估 notebook 的特殊跑法。生产单条 query 预期：

| 场景 | 预期耗时 | 说明 |
|------|----------|------|
| 首次提问（冷启动） | **~3–8 分钟** | 与首轮单条接近；含 rerank + r1 推理 |
| 相同问题复问（生成缓存命中） | **~2–4 分钟** | 当前仍做全量检索；优化检索缓存后可降到 **几十秒–2 分钟** |
| 换更快模型（非 r1 的 7B） | 生成 **30s–2min** | r1 推理链是主要瓶颈 |
| `skip_rerank=True`（快速模式） | 省 **十几秒–1 分钟** | 牺牲排序质量 |

评估 vs 生产的差异：

```text
本次 C5（评估）                    生产 RAG（理想态）
─────────────────────────────────────────────────────
8 次任务（4+4 两轮）              通常每次 1 条 query
每次重新组装 pipeline             服务启动时加载一次
测缓存 + 测评估指标               用户只关心返回答案
deepseek-r1:7b（慢推理）          可换更快模型
检索每次都跑（cache 不省检索）    可加检索结果缓存
分片 BM25 冷读 61 片              可预热 / 常驻 / 优化查询
```

### 优化方向（按收益排序）

1. **换更快的生成模型**：r1 推理链最耗时，换非推理型 7B 可显著降 latency。
2. **加检索结果缓存**：让 cache hit 时跳过全量检索（当前二轮 ~3.5 分钟大头是检索）。
3. **分片 BM25 查询常驻/预热**：避免每次冷读 61 个 pickle；可服务启动时载入或做内存缓存。
4. **服务常驻**：pipeline、reranker、embedder 只加载一次，不随每次 query 重建。
5. **按需 `skip_rerank`**：低时延场景下用融合结果直出。

**结论**：`23 分钟 = 评估成本`；单条首次全量 live 问答 **~5–8 分钟** 合理，产品化后可压到 **30 秒–2 分钟** 量级。真正要盯的两个瓶颈是 **LLM 推理** 和 **检索（含分片 BM25 冷读）**。

---

## Q6：分片 BM25 相比单体 BM25 影响有多大？（阶段 7 C4.5 实测）

> 背景：全量 BM25 因内存爆掉改成**分片构建**（每片单独建 `BM25Okapi`），带来「各片 IDF 基于本片文档频率、跨片原始分数略不可比」的近似。C4.5 用数据量化其影响。

### 分片近似的原理（一句话）

BM25 的 IDF 依赖**整库统计**（总文档数 N、含词文档数 df）。单体索引全库共用一套 IDF，跨文档分数可比；分片后每片只用**本片**的 N 和 df，同一个词在不同片的 IDF 不同，**跨片原始分数不能直接比大小**。

### 为什么「用于召回候选」可接受

BM25 只是多路召回的一路，后面还有融合 + rerank 兜底：

```text
Query
  ├─ 向量路 (Chroma)      → 候选
  ├─ 关键词路 (BM25 分片)  → 候选
  └─ 融合 (RRF) → rerank 复排 → 最终上下文
```

- **RRF 融合看排名不看绝对分数** → 分片导致的 raw score 不可比被部分「洗掉」。
- **reranker 重新打分**，不依赖 BM25 分数 → 只要相关文档进了候选池，顺序主要由 rerank 决定。
- 真正要盯的风险其实是**每片 top_k 截断导致的召回遗漏**（相关文档在某片排名太靠后被截掉），而非 IDF 可比性。

### C4.5 实测数据（top-10 重叠率）

**A 组｜同语料对照（样本库上，分片 vs 单体）** —— 隔离分片算法误差：

| Query | 重叠 |
|-------|------|
| MI treatment | 10/10 |
| metformin cardiovascular | 10/10 |
| malaria 2015+ | 8/10 |
| warfarin elderly | 10/10 |
| **avg_overlap_rate** | **0.95** |

→ **同语料下分片与单体 keyword top-10 高度一致，分片近似影响很小。**

**B 组｜全量分片 vs 样本单体** —— 工程切换视角：

| Query | 重叠 |
|-------|------|
| 全部 4 条 | 0/10（avg 0.0） |

→ 这是**预期**结果：610 万库与 1267 条样本库的候选 chunk 几乎完全不同，B 组只反映「上全量后 keyword 召回大变」，**不能**用来判断分片好坏。

### 结论

- 分片 BM25 牺牲的是「全局 IDF 一致性 + BM25 内部精确排序」，换来**可构建、可断点续建、低内存**。
- A 组 0.95 重叠证明：在「双路召回 → 融合 → rerank」架构下，分片近似对最终质量影响可忽略。
- 若要严格全局 IDF，可做「先流式统计全局 df，再分片建索引用统一 IDF」，但内存与实现成本更高，当前无必要。
- 复现入口：notebook **C4.5**，产物 `outputs/samples/bm25_sharded_vs_mono_overlap.json`；只看分片误差可设 `RUN_FULL_CORPUS_BM25_COMPARE = False` 跳过较慢的 B 组。

---

*后续 Q&A（实现过程中补充）：ROUGE 版本选型、ground truth 编写规范、缓存键 temperature_bucket 粒度、BatchRunner 与 Ollama 并发实测等。*
